# AI Buckets Dashboard

Public dashboard tracking equal-weighted AI beneficiary and disrupted category indexes, indexed to $100 over the trailing two years.

Data source: Yahoo Finance via `yfinance`, using dividend/split-adjusted `Adj Close`.
